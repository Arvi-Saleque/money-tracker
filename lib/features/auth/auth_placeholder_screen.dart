import '../../shared/widgets/app_placeholder_screen.dart';

class AuthPlaceholderScreen extends AppPlaceholderScreen {
  const AuthPlaceholderScreen({super.key})
    : super(
        title: 'Authentication',
        description:
            'Phase 2 will add login, sign up, password reset, and route guards here.',
      );
}
