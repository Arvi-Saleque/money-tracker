import '../../shared/widgets/app_placeholder_screen.dart';

class TransactionsPlaceholderScreen extends AppPlaceholderScreen {
  const TransactionsPlaceholderScreen({super.key})
    : super(
        title: 'Transactions',
        description:
            'Phases 3 and 4 will add transaction creation, history, search, filters, and sorting here.',
      );
}
