import '../../shared/widgets/app_placeholder_screen.dart';

class GoalsPlaceholderScreen extends AppPlaceholderScreen {
  const GoalsPlaceholderScreen({super.key})
    : super(
        title: 'Goals',
        description:
            'Phase 10 will add savings goals, contributions, and progress tracking here.',
      );
}
