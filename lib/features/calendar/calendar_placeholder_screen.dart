import '../../shared/widgets/app_placeholder_screen.dart';

class CalendarPlaceholderScreen extends AppPlaceholderScreen {
  const CalendarPlaceholderScreen({super.key})
    : super(
        title: 'Calendar',
        description:
            'Phase 6 will add month and week views with transaction indicators and day details here.',
      );
}
