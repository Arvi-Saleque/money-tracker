import '../../shared/widgets/app_placeholder_screen.dart';

class BudgetsPlaceholderScreen extends AppPlaceholderScreen {
  const BudgetsPlaceholderScreen({super.key})
    : super(
        title: 'Budgets',
        description:
            'Phase 8 will add budget limits, progress bars, and alerts here.',
      );
}
