import '../../shared/widgets/app_placeholder_screen.dart';

class SubscriptionsPlaceholderScreen extends AppPlaceholderScreen {
  const SubscriptionsPlaceholderScreen({super.key})
    : super(
        title: 'Subscriptions',
        description:
            'Phase 9 will add recurring bills, reminders, and payment tracking here.',
      );
}
