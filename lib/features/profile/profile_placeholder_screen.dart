import '../../shared/widgets/app_placeholder_screen.dart';

class ProfilePlaceholderScreen extends AppPlaceholderScreen {
  const ProfilePlaceholderScreen({super.key})
    : super(
        title: 'Profile',
        description:
            'Phase 2 will add editable profile, avatar, currency, language, and theme settings here.',
      );
}
